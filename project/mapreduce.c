
int main(int argc, char* argv[]) {
    int n, m, k;
    scanf("%d", &n);
    int matrix[n][n];
    for(int i = 0; i < n; i++) {
        for(int j = 0; j < n; j++) {
            scanf("%d", matrix[i][j]);
        }
    }
    scanf("%d %d", &m, &k);
}